{
  "ConnectionStrings": {
    "DefaultConnection": "Data Source = localhost;Initial Catalog = UniversityTest;Integrated Security=True; Trust Server Certificate=True"
  }
  // Soulution exploer -> appSettings.json -> properties -> copy if newer
}